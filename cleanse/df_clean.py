
import pandas as pd
def clean(imp):
    #json_file = open(path+json_name+'.json', 'w', encoding='utf-8')   
    for each in imp:
        each['review_id']
    #json_file.write(json.dumps(tmp , ensure_ascii=False,indent = 4))
    # df = imp
    # result = []
    # df2 = pd.DataFrame()
    # for data,item in df.iterrows():
    #     tmp = {}
    #     tmp_list = []
    #     tmp_list.append(str(data))
    #     tmp_list.append(str(item['member_id']))
    #     value = item['result_data']
    #     for i in value[:-1]:
    #         print(i)
    #         try:
    #             key = list(i.keys())[0]
    #             tmp_list.append(i[key]) 
    #         except:
    #             pass
    #     series = pd.Series(tmp_list)
    #     df2 = df2.append(series,ignore_index=True)
    # print(df2)
    # df2.columns = ['data_idx','member_id',"휴대전화 번호",'Q1. 채식경험이 있습니까?','Q2. 채식주의와 비거니즘의 차이를 알고 있습니까?',"Q3. 동물권리(동물 학대 감소)에 대해 알고 있습니까?",
    # "Q4. 비거니즘이라는 단어를 들어본 적이 있습니까?","Q5. 친환경에 관심이 있고 실천하고 있습니까?","Q6. 채식 식당을 이용해본 적이 있습니까?","Q7. 제품에 있는 '비건 인증'을 본 적이 있습니까?",
    # "Q8. 비건 인증을 받은 상품이 일반 상품보다 가격이 조금 더 높을 경우 구매할 의사가 있습니까?","Q9. 학교, 관공서, 군대에 비건 메뉴 도입이 필요하다고 생각하십니까?","Q10. 일반인을 대상으로 한 비거니즘 홍보(교육)가 필요하다고 생각하십니까?"]
    # df2.to_excel('/data/collection/prj2977.xlsx')

